# Innovadataco Admin — variables de entorno
NODE_ENV=production
PORT=5001
HOSTNAME=0.0.0.0

# Base de datos PostgreSQL
DATABASE_URL=postgresql://postgres:postgres@localhost:5436/innovadataco?schema=public

# JWT (para auth futura)
JWT_SECRET=cambia_esto_en_produccion
