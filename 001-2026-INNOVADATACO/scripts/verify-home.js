const puppeteer = require("puppeteer"); const path = require("path"); const fs = require("fs"); const sleep = ms => new Promise(r => setTimeout(r, ms)); (async () => { const browser = await puppeteer.launch({ headless: "new", args: ["--no-sandbox", "--disable-setuid-sandbox"] }); const page = await browser.newPage(); await page.setViewport({ width: 1400, height: 900 }); const screenshotsDir = path.join(__dirname, "..", "screenshots"); if (!fs.existsSync(screenshotsDir)) { fs.mkdirSync(screenshotsDir, { recursive: true }); } console.log("=== VERIFICACION HOME Y MODULOS ==="); console.log("1. Login..."); await page.goto("http://localhost:5000/login", { waitUntil: "networkidle2" }); await sleep(500); await page.type("input[placeholder=admin]", "admin"); await page.type("input[type=password]", "admin123"); await page.click("button[type=submit]"); await page.waitForNavigation({ waitUntil: "networkidle2" }); await sleep(1000); await page.screenshot({ path: path.join(screenshotsDir, "04-home-placeholder.png"), fullPage: true }); console.log("Captura: / con placeholder"); const hasPlaceholder = await page.evaluate(() => document.body.innerText.includes("Selecciona un módulo")); console.log("Placeholder visible:", hasPlaceholder, hasPlaceholder ? "OK" : "FAIL"); console.log("2. Click en Base Oficial..."); await page.click("button:has-text(\"Base\")"); await sleep(1500); await page.screenshot({ path: path.join(screenshotsDir, "05-modulo-base.png"), fullPage: true }); console.log("Captura: Modulo Base Oficial"); const hasSubmodulos = await page.$$eval("button", btns => btns.some(b => b.innerText.includes("Empresas") || b.innerText.includes("Consulta"))); console.log("Submodulos visibles:", hasSubmodulos, hasSubmodulos ? "OK" : "FAIL"); await browser.close(); console.log("COMPLETADO"); })();