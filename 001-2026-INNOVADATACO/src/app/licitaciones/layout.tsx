export const metadata = {
  title: "Módulo de Licitaciones - INNOVADATACO",
  description: "Gestión de licitaciones y documentos",
};

export default function LicitacionesLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
