# SPEC-587 — plantilla mínima de .env.test (gitignored; el archivo real vive fuera de git).
# Los tests de OAuth usan valores de relleno: los exportan ellos mismos si faltan.
GOOGLE_CLIENT_ID="(provisionar)"
GOOGLE_CLIENT_SECRET="(provisionar)"
