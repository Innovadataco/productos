# Data Model: Tests de rol + documentación de arquitectura

**Date**: 2026-07-20
**Feature**: specs/047-tests-rol-arquitectura/spec.md

---

No aplica — este spec no modifica el modelo de datos de Prisma.

Solo añade:
- Tests de Vitest en `src/lib/role-visibility.test.ts`.
- Documentación de arquitectura en `docs/ARCHITECTURE.md`.
- Anotaciones JSDoc en módulos existentes de `src/lib/`.

No se requieren migraciones ni cambios en `prisma/schema.prisma`.
