# Research — SPEC-209

## Decisiones ya cerradas por ZEUS / brief

1. **Invertir paleta del bloque humano**: `bg-tinta/90` + `text-fondo`, consistente con bloque JSON.
2. **Cambio mínimo**: solo `LogContextoModal.tsx`.

## Patrones del repo a reutilizar

- Componente existente `src/components/modules/monitoreo/LogContextoModal.tsx`.
- Tailwind tokens `bg-tinta/90`, `dark:bg-tinta/95`, `text-fondo`.
