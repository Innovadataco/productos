services:
  app:
    image: node:22
    container_name: pgn-simulacro
    working_dir: /app
    volumes:
      - .:/app
    ports:
      - "127.0.0.1:5018:3000"
    command: sh -c "npm install && npm run db:seed && npm run db:load-content && npm run build && npm start"
    restart: unless-stopped
