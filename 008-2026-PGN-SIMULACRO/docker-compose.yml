version: '3.8'

services:
  nginx:
    image: nginx:alpine
    container_name: pgn-estudio-nginx
    ports:
      - "80:80"
    volumes:
      - ./dist:/usr/share/nginx/html:ro
    restart: unless-stopped
